{
  "name": "sass-platform-logs-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for querying SASS platform system logs, dry run logs, and automation logs",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1024.0",
    "@modelcontextprotocol/sdk": "^1.0.4",
    "mongodb": "^6.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
